/**
 * types/index.ts — Re-exports from @advl/shared for use within core
 *
 * Core components should import types from here, not directly from @advl/shared,
 * so that the import path stays consistent within the package.
 */
export * from '@advl/shared'
